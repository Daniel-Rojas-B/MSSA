<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SoccerBettingApp.ViewModel"
             x:Class="SoccerBettingApp.View.BetPage"
             Title="My Bets">
    

    <CollectionView ItemsSource="{Binding Bets}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="10" Margin="5" CornerRadius="8">
                    <StackLayout>
                        <Label Text="{Binding MatchName}" FontAttributes="Bold"/>
                        <Label Text="{Binding SelectedOutcome}"/>
                        <Label Text="{Binding Amount, StringFormat='{}{0:C}'}"/>
                        <Label Text="{Binding PlacedAt, StringFormat='{}{0:MMM dd, yyyy h:mm tt}'}"
                   FontSize="Small" TextColor="Gray"/>
                    </StackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>


